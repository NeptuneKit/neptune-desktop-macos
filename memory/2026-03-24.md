# 2026-03-24

- 记录：`neptune-desktop-macos` 的 inspector 加载逻辑已支持 `NEPTUNE_INSPECTOR_DIST`、仓库外 `../neptune-inspector-h5/dist`、app bundle 内 `Resources/inspector/index.html`，最后回退到 `http://127.0.0.1:18765/`。
- 验证：`swift build` 通过，`swift test` 通过。
- 决策：新增 `scripts/package-macos-app.sh` 作为最小发布闭环，使用 `swift build` + `ditto` 组装 `.app`，并把 `neptune-inspector-h5/dist` 同步到 app bundle `Resources/inspector/` 与 SwiftPM `Bundle.module` 资源 bundle 中，避免手动 copy step。
